---
bug_id: BUG-20260219-001
status: investigating
created: 2026-02-19T00:00:00Z
updated: 2026-02-19T00:00:00Z
symptoms: Docker compose build fails due to TypeScript compile errors in backend and worker
root_cause: 
fix: 
---

# Debug: Docker compose startup blocked by TypeScript compile failures

## Symptoms (IMMUTABLE — never edit after initial write)
- `docker compose -f infrastructure/docker-compose.yml -f infrastructure/docker-compose.override.yml up -d --build` does not complete successfully.
- Backend Docker build fails during `npm run build` with multiple TypeScript errors.
- Worker Docker build fails during TypeScript compile with ioredis constructor typing error (`Type has no construct signatures`).
- Expected: compose stack builds/starts and smoke endpoints respond from host.

## Current Focus (OVERWRITE — always shows current state)
**Hypothesis:** Build failures are caused by at least two independent TypeScript issues: worker ioredis import style mismatch and backend type-level regressions.
**Testing:** Reproduce with exact compose command, capture concrete errors, then isolate/fix worker first.
**Evidence so far:** Initial user report and prior failed worker build command in terminal context.

## Eliminated Hypotheses (APPEND-ONLY)

## Evidence Log (APPEND-ONLY)
| # | Observation | Source | Implication |
|---|---|---|---|
| 1 | Prior `docker compose ... build worker` exited with code 1 | Terminal context | Worker TypeScript compile failure is reproducible and blocks compose build |

## Resolution (OVERWRITE — filled when fixed)
**Root Cause:** 
**Fix:** 
**Verification:** 
**Regression Risk:** 
